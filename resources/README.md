Placeholder for shelljar branding assets (icons, pfp, fonts).

Drop files in this dir and reference them from `../../qml/Config.qml`
(e.g. `resources/logo.png`) — the package copies this whole dir to
`$out/resources`.